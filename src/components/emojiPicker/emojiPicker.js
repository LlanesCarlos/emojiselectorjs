import { forwardRef } from "react";

export function EmojiPicker(props, inputRef){
    return <div>
        <button>😀video en 1:47:20</button>
    </div>;
}

export default forwardRef(EmojiPicker);